# Okta Configuration
# Required: Your Okta organization URL (e.g., https://dev-123456.okta.com)
OKTA_ORG_URL=https://dev-123456.okta.com

# Required: Your Okta API token (SSWS token with permissions to read users, groups, and app links)
OKTA_API_TOKEN=your-ssws-token-here

# API Security
# Optional: API key for webhook authentication (recommended for production)
API_KEY=your-secret-api-key-here

# Logging Configuration
# Optional: Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Optional: Log format (json or text)
LOG_FORMAT=json

